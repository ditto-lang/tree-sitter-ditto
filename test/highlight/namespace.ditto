module Test exports (..)
--      ^ namespace


import Some.Module as SM
--      ^ namespace
--                    ^ namespace

id = SM.identity
--   ^ namespace

unwrap = fn (SM.Pair(x, y)) -> [x, y, SM.z]
--           ^ namespace
--                                     ^ namespace
