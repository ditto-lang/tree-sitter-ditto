module Test exports (
    Foo,
    -- <- type
    Bar(..),
    -- <- type
);

type alias T = SomeModule.T(Int);
--         ^ type
--                        ^ type
--                           ^ type


type alias Identity(a) = (a) -> a;
--                           ^ type
